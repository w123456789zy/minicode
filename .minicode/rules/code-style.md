# 编码风格

- 缩进：4 空格，不用 tab
- 行宽：100 字符以内
- 字符串：双引号优先
- 命名：snake_case（变量/函数）、PascalCase（类）
- 类型注解：函数签名必须带；内部变量按需
- 文档字符串：模块级 + 公共类/函数
- import 顺序：标准库 → 第三方 → 本地（用 isort 风格）

## 错误处理
- 用户输入边界：必须校验
- 内部调用：信任，但捕获 + 包装成有意义的错误
- 不要用 bare `except:`，至少 `except Exception:`
- 异常信息要给"做了什么"+"接下来怎么办"
